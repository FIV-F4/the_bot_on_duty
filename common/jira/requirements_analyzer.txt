# Зависимости для анализатора JIRA
# Работает как самодостаточный файл без внешних зависимостей проекта

aiohttp>=3.8.0          # Асинхронные HTTP запросы к JIRA API
beautifulsoup4>=4.11.0  # Парсинг HTML страниц Service Desk
lxml>=4.9.0             # Парсер XML/HTML для BeautifulSoup
requests>=2.28.0        # Синхронные HTTP запросы (резервный вариант)

# Опциональные зависимости для расширенного анализа
pandas>=1.5.0           # Анализ данных (опционально)
matplotlib>=3.6.0       # Графики (опционально) 